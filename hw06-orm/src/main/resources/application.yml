spring:
  datasource:
    url: jdbc:h2:mem:library
    driverClassName: org.h2.Driver
    username: sa
    password:
    initialization-mode: never
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    show-sql: true

liquibase:
  enabled: true
  url: jdbc:h2:mem:library
  driverClassName: org.h2.Driver
  user: sa
  password:
